import React from 'react';

const Loading = () => {
    return (
        <div className='text-center font-bold text-4xl primary-clr bg-clip-text text-transparent min-h-screen flex items-center justify-center'>
            <p>Loading...</p>
        </div>
    );
};

export default Loading;